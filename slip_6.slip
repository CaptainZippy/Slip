

(define test (func celsius (F) (mul (div 5 9) (sub F 32) )))

(print "H" wrap (type wrap))
(print (type test))

(func normalize (expr)
  (switch expr
    ((eq? (type expr) "function")
     "OHOHO")
    (1
     "yes")))

(print "ORIG" test)
(print "NORM" (normalize test))

;(func test (n)
;  (match n
;    (0 "zero")
;    (1 "one")
;    (2 "two")
;    ((3 4 5) "big")
;    (_ "other")))
