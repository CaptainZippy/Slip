
;(define harmonic (lambda (n : double)
;    (let ((acc n)) acc)))

(func harmonic (n : double)
    (let ((acc n)) acc))
    
;begin
;   (for i (range 1 n)
;      (set! acc (add_f (div_f 1.0 (float i)) acc)))
; acc))))
    
;(define main (lambda (argv)
;    (map (lambda (x) (print "Harmonic" x (harmonic x))) argv)))

(func main ()
    (harmonic 4))