; TODO Ranges

(define main (argv:(array string_view)):int
    (let x 0)
    (let n (if (ge_i? argv.size 0)
        1 (atoi (argv 0))))
    (for a (range n)
        (for b (range n)
            (for c (range n)
                (for d (range n)
                    (for e (range n)
                        (for f (range n) (inc! x)))))))
    (print x))
