
(define fib (lambda (n)
    (cond
        ((lt_i? n 2)
             1)
        (true
            (add_i
                (fib (sub_i n 1))
                (fib (sub_i n 2)))))))

(define main (lambda (argv)
    (map (lambda (x) (print "Fib" x (fib x))) argv)))

