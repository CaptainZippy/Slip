; Good

(func fib (n:int):int
    (if (lt? n 2)
        1
        (add
            (fib (sub n 1))
            (fib (sub n 2)))))

(func main (args:(array_view string)):int
    (var n (if (lt? (size args) 2) 13 (atoi (at args 1))))
    (puti n)
    (puti (fib n))
    0)

