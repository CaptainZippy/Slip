
(define fib (lambda (n)
    (cond
        ((lt? n 2)
             1)
        (true
            (add
                (fib (sub n 1))
                (fib (sub n 2)))))))

(define main (lambda (argv)
    (map (lambda (x) (print "Fib" x (fib x))) argv)))

